const name = "Анна";
const age = 25;

// Старый способ (конкатенация)
let oldWay = "Привет, " + name + "! Тебе " + age + " лет.";

// Новый способ (шаблонные строки)
let newWay = `Привет, ${name}! Тебе ${age} лет.`;

// Можно вставлять любое выражение
let calculation = `Через 5 лет тебе будет ${age + 5} лет.`;

// И многострочность без \\n
let multiLine = `
    Это многострочная
    строка. Очень удобно
    для HTML-шаблонов.
`;
console.log(multiLine);